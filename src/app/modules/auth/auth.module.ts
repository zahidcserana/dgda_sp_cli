export interface UserData {
    user_id: number;
    name: string;
    email: string;
    token: string;
    location_id: string;
    terminal_id: string;
    user_type_id: number;
    store_id: number;
}